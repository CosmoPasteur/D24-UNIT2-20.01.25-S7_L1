const form = document.getElementById("formShop"); //abbiamo il nodo del form (è un oggetto js)

class User {
  constructor(firstName, lastName, age, location) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
    this.location = location;
  }

  confront;
}
